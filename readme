
Open the directory in terminal and go to the app directory and execute the following command 
set FLASK_APP={APP_NAME}

Run the following command to start the Flask Server

flask run

To run Flask Server with hot reload use this command
flask --debug run

To create new AI Model
remove the existing model in the '/model' directory 

Working AI Model
Sklearn - Random Forest